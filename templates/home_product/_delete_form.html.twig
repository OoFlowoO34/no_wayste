<form method="post" action="{{ path('app_home_product_delete', {'id': home_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ home_product.id) }}">
    <button class="btn">Delete</button>
</form>


